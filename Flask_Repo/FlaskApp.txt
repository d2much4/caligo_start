Flask and Azure...

#Optional step to clone an existing repository locally...
[Bash] 

# (1)-create a virtual environment...
python3 -m venv venv
# (2)-activate the virtual environment...
source venv/bin/activate
# (3)-install requirement into the virtual environment...
pip install -r requirements.txt
# (4)-start the app...
FLASK_APP=application.py flask run
